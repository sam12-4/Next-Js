import React from 'react'

const page = () => {
  return (
    <div>
      Admin login
    </div>
  )
}

export default page
export async function generateMetadata({ params }) {
  return {
    title: ' Admin login',
  }
}
